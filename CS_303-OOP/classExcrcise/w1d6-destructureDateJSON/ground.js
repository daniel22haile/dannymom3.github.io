"use strict";

function someFun(num) {
    let jordan = 23;
    if (num >= 0) {
        num = Math.sqrt(num)
    }

    start = jordan + num;
    con
    sole.log(num + start);
}

function getQ(name) {
    return 6;
}

const m = getQ(hi);