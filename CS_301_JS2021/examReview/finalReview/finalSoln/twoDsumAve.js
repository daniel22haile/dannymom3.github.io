"use strict";

function sumAve(arr) {
    let sum = 0;
    for (let i = 0; i < arr.length; i++) {
        let fillMe = [];
        for (let j = 0; j < arr[i].length; j++) {
            if (arr[i] += arr[j])
                sum += arr[i] arr[j];
        }
    }
}